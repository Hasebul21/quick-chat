{
  "name": "quick-chat",
  "private": true,
  "description": "Local convenience: spawns the full stack via Docker Compose (see README).",
  "scripts": {
    "start": "docker compose up --build",
    "up": "docker compose up"
  }
}
