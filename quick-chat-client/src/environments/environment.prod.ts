// Overwritten by `node scripts/write-env.cjs` when `QUICKCHAT_API_BASE` is set (e.g. on Vercel).
export const environment = {
  production: true,
  apiBaseUrl: 'https://quick-chat-production-3aee.up.railway.app',
};
